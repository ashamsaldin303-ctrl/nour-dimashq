# ASSUMPTIONS.md — Nour Dimashq M1

Logged per brief Rule zero ("never invent"). Format: id · decision · rationale · risk · blast radius · reversal.
Cap: 5 entries.

| ID | Decision | Rationale | Risk | Blast radius | Reversal |
|---|---|---|---|---|---|
| A-1 | SQLite storage engine substituted for the brief's PostgreSQL, with mechanical type mapping: enums→String columns validated by shared Zod unions (`src/lib/schemas.ts`); `features String[]`→CSV; `media Json`→JSON string parsed+validated in `src/lib/listings.ts`; `priceUsd Decimal`→Int (whole dollars); `priceSyp Decimal`→BigInt | The sandbox provides SQLite only (environment constraint recorded in worklog Task 0). App-layer data contract (entities, fields, constraints, validation) is preserved byte-for-byte at the boundary | Schema divergence confuses a future maintainer; SQLite lacks DB-level enum enforcement | Prisma schema + adapters only — no UI/API changes | Switch `provider` to "postgresql", restore `enum` blocks + `@db.Decimal` + `String[]` + `Json` from brief §6; app layer unchanged |
| A-2 | Verification battery runs on the dev server + browser automation + curl/grep instead of Vitest/Playwright/LHCI files | Sandbox law: no test files, no `next build`. Every AC keeps its named check with evidence under `/evidence/<AC-ID>/`; production-build-only numbers (Lighthouse) are labeled dev-measured or UNVERIFIED, never claimed | Prod-only perf gates (AC-2/3/4/5) can't be reproduced exactly | Verification harness only — zero product code | On a build-capable machine: run brief §10 battery verbatim |
| A-3 | Seed marks 4 listings `isSignature` (brief table marks 3: APR-118, APR-102, VIL-031); added HOU-2026-044 | Brief §4 requires the home signature collection to show 4–6 cards "from seed isSignature", but §6 table marks only 3 — an internal inconsistency; HOU-044 (deed_checked, distinctive Damascus house) fits curation | One extra card appears in the signature row | Home page signature section + 1 seed flag | Unset `isSignature` on HOU-2026-044 and re-run seed |
| A-4 | `NEXT_PUBLIC_WHATSAPP_NUMBER=963991234567` (dev) — the SAMPLE number from the brief's own exemplar | `lib/wa.ts` must never hardcode the number (NEVER-10); dev needs a working value; the number is marked SAMPLE in `.env.example` + CONTENT.md swap path | Someone ships the sample number to production | All wa.me links | Set the real number in `.env` before launch (CONTENT.md register) |
| A-5 | `og:image` = the static home poster for all routes (no per-PDP dynamic OG kit) | Per-PDP OG needs satori + an Arabic-capable font file; the sandbox has no Arabic system fonts (fc-list empty) and runtime font fetch violates the font law. Brief marks the OG kit as SHOULD, not MUST | PDP social cards are generic | Metadata only | Add `opengraph-image.tsx` with embedded Arabic font when deployed on a build-capable host |
